import ReactOnRails from 'react-on-rails';

import CharitySearch from '../bundles/CharitySearch/components/CharitySearch';

// This is how react_on_rails can see the CharitySearch in the browser.
ReactOnRails.register({
  CharitySearch,
});
